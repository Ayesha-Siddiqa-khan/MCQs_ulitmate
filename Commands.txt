cd "F:\Mobile apps projects\MCQs_ulitmate\backend"
& ".\.venv\Scripts\python.exe" -m uvicorn app.main:app --reload --port 8000

cd "F:\Mobile apps projects\MCQs_ulitmate\frontend"

npm run dev

# NOTE:
#   backend\.env        = private secrets (Supabase service role, JWT secret, AI fallbacks, ...)
#   frontend\.env.local = public NEXT_PUBLIC_* values only (Supabase URL, anon key, API base URL)
#   Do NOT put service-role / AI / JWT / DB keys in frontend\.env.local.
